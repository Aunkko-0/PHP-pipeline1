# ใช้ PHP 8.2 พร้อม Apache Web Server
FROM php:8.2-apache

# เปิดใช้งาน mod_rewrite (จำเป็นสำหรับ Framework หรือ URL สวยๆ)
RUN a2enmod rewrite

# ติดตั้ง Extension ที่จำเป็น
RUN docker-php-ext-install mysqli pdo pdo_mysql

# กำหนดโฟลเดอร์ทำงาน
WORKDIR /var/www/html

# -----------------------------------------------------------
# 👇 จุดที่เพิ่ม: ก๊อปปี้โค้ดจากเครื่อง Jenkins เข้าไปใน Container
# -----------------------------------------------------------
COPY . /var/www/html/

# (Optional) ตั้งค่า Permission ให้ Apache อ่าน/เขียนไฟล์ได้ (ถ้าจำเป็น)
# RUN chown -R www-data:www-data /var/www/html